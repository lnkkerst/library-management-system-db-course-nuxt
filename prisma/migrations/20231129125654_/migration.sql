BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ReaderType] ALTER COLUMN [bookQuota] INT NULL;
ALTER TABLE [dbo].[ReaderType] ALTER COLUMN [borrowPeriod] INT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
